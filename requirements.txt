fastapi
httpx
pyyaml
python-multipart
docx2python
openpyxl
pdfplumber
python-pptx
email-reply-parser
requests
jsonschema
referencing
cryptography>=43.0
# Real PDF table/chart extraction (parse_pdf) is an optional, much heavier
# add-on -- see requirements-pdf-ml.txt. Not required: parse_pdf falls
# back to plain text extraction when it's absent.
python-docx
# Test-only: parse_docx itself uses docx2python (above); python-docx is
# needed solely to build realistic .docx fixtures in test_pan103_docx_tables.py.
