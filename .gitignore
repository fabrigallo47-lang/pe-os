# derived index — never authoritative, rebuilt via `make index`
.index/

.venv/
__pycache__/
*.pyc
.DS_Store

# Obsidian workspace state (keep vault content, not app state)
vault/.obsidian/workspace*.json
vault/.obsidian/cache/
.models/
.vercel
.env.local

# Local Claude Code settings
settings.local.json

# Deal data — excluded from public repo (populate via /open-deal + /ingest)
vault/deals/
vault/inbox/
vault/audit/
vault/entities/people/

# Domain source documents and private research
sources/

# Operating manual (internal AI session log)
AI_HARNESS.md

# Secrets / keys
.env
*.key
.env*

# Report lasciati da check_all.py del kit PANTA (derivati, non nostri)
independent_validation_report*

# UI V17: consegna di design, pesante e non nostra da versionare
ui/demo/
ui/references/
ui/legacy/

# Derived by ingest, safe to delete: the live staging store and the cache of
# computed cell values (keyed by the workbook's content digest).
pipeline_out/live/
pipeline_out/cellvalues/
