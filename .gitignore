# derived index — never authoritative, rebuilt via `make index`
.index/

.venv/
.venv
__pycache__/
*.pyc
.DS_Store

# Obsidian workspace state (keep vault content, not app state)
vault/.obsidian/workspace*.json
vault/.obsidian/cache/
.models/
.vercel
.env.local

# Local Claude Code settings
settings.local.json

# Deal data — excluded from public repo (populate via /open-deal + /ingest)
vault/deals/
vault/inbox/
vault/audit/
vault/entities/people/

# Domain source documents and private research
sources/

# Operating manual (internal AI session log)
AI_HARNESS.md

# Secrets / keys
.env
*.key
.env*

# Report lasciati da check_all.py del kit PANTA (derivati, non nostri)
independent_validation_report*

# UI V20: old V17 exclusions no longer apply — everything in ui/ is tracked

# Derived by ingest, safe to delete: the live staging store and the cache of
# computed cell values (keyed by the workbook's content digest).
pipeline_out/live/
pipeline_out/cellvalues/
# Keystone V20 extraction output: local, derived and may contain deal evidence.
pipeline_out/e3/K-IC/adapter_alpha/
# Per-case V20 runtime bundles (non-keystone cases): same category as above.
pipeline_out/cases/
logs/

# Downloaded benchmark corpora and derived evaluation runs.
.panta-eval/

# Proprietary algorithms — not for public repo
tools/case_compiler_alpha.py
tools/b0_assembler.py
tools/model_compiler.py
tools/model_parser.py
tools/keystone_model.py
tools/binding_proposer.py
tools/binding_resolver.py
tools/position_model_binder.py
tools/benchmark_runner.py
tools/benchmark_extract.py
tools/grade_keystone.py
tools/coverage_report.py
tools/claim_graph.py
tools/cell_engine.py

# Internal docs — not for sharing without NDA
docs/ui_audit.md
vault/policy/keystone_authority_matrix_v0.json
vault/policy/keystone_materiality_policy_v0.json
